query ($id: String!) {
    findById(id: $id) {
        id
        createdDate
        modifiedDate
        firstName
        lastName
        dateOfBirth
        gender
        email
        status
        enabled
        #password //this is hiden
    }
}
